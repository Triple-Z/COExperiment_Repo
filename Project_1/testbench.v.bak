`include "mips.v"

module testbench ();
	reg clk, rst;

	initial begin
		clk = 0;
		rst = 1;
		#12 rst = 0;
	end

	always @ ( * ) begin
		#10 clk = ~clk;
	end

	mips mips(
		.clk(clk),
		.rst(rst)
	);

endmodule // Test Bench;
